<!DOCTYPE html>
<html lang="en" class="dark">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Communnity — Verified Business Data for India</title>
    <meta name="description" content="Communnity: Compliance-first B2B data for India — datasets, enrichment API, monitoring signals." />
    <link rel="canonical" href="<%= request.original_url %>">
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "Organization",
        "name": "Communnity",
        "url": "https://www.communnity.org",
        "sameAs": [],
        "logo": "https://www.communnity.org/favicon.ico"
      }
    </script>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload" %>
  </head>
  <body class="min-h-screen flex flex-col">
    <header class="site-header">
      <div class="site-header-inner">
        <div class="flex items-center gap-3">
          <%= render "shared/brand_logo" %>
          <a href="/" class="font-semibold text-xl tracking-tight">Communnity</a>
        </div>
        <nav class="hidden md:flex items-center gap-6 text-sm"></nav>
        <div class="hidden md:flex items-center gap-3">
          <% if logged_in? %>
            <details class="relative">
              <summary class="list-none cursor-pointer px-3 py-2 border rounded">Account ▾</summary>
              <div class="absolute right-0 mt-2 w-48 bg-white border rounded shadow p-2 text-sm">
                <div class="px-2 py-1 text-gray-600">Hi, <%= current_user.name %></div>
                <a class="block px-2 py-1 hover:bg-gray-50" href="/account">My Account</a>
                <%= button_to "Logout", logout_path, method: :delete, class: "w-full text-left px-2 py-1 hover:bg-gray-50" %>
              </div>
            </details>
          <% else %>
            <a href="/login" class="btn-brand text-sm">Login</a>
            <a href="/signup" class="btn-brand-outline text-sm">Sign up</a>
          <% end %>
        </div>
        <details class="md:hidden">
          <summary class="list-none cursor-pointer px-3 py-2 rounded border">Menu</summary>
          <div class="mt-2 bg-white border rounded p-3 flex flex-col gap-2 text-sm">
            <% if logged_in? %>
              <a href="/account" class="link-brand">My Account</a>
              <div class="text-sm text-gray-600">Hi, <%= current_user.name %></div>
              <%= button_to "Logout", logout_path, method: :delete, class: "btn-brand-outline text-sm" %>
            <% else %>
              <a href="/login" class="btn-brand text-sm text-center">Login</a>
              <a href="/signup" class="btn-brand-outline text-sm text-center">Sign up</a>
            <% end %>
          </div>
        </details>
      </div>
    </header>

    <% if notice %>
      <div class="bg-green-50 text-green-800 px-4 py-3 text-sm"><%= notice %></div>
    <% end %>
    <% if alert %>
      <div class="bg-red-50 text-red-800 px-4 py-3 text-sm"><%= alert %></div>
    <% end %>

    <main class="flex-1">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
        <%= yield %>
      </div>
    </main>

    <footer class="bg-white border-t dark:bg-gray-900 dark:border-gray-800">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 grid gap-6 md:grid-cols-3 text-sm">
        <div>
          <div class="font-semibold mb-2">Communnity</div>
          <div>Address: Bengaluru, KA — 560001</div>
        </div>
        <div class="space-y-1">
          <a class="block hover:text-blue-600" href="/privacy">Privacy</a>
          <a class="block hover:text-blue-600" href="/terms">Terms</a>
          <a class="block hover:text-blue-600" href="/compliance">Compliance</a>
        </div>
        <div class="space-y-2">
          <a href="#lead" class="inline-block btn-brand">Book a Demo</a>
          <a href="/contact" class="ml-2 inline-block px-3 py-2 rounded border border-gray-300 hover:bg-gray-50">Contact Sales</a>
        </div>
      </div>
    </footer>
  </body>
</html>
